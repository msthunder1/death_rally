/**
 * TRACK CONFIGURATION
 *
 * Visual defaults for track rendering.
 * Individual tracks can override some of these via their definition.
 */

export const TrackConfig = {
    // Road
    roadColor: 0x3a3545,
    roadTextureColor: 0x4a4555,

    // Walls
    wallColor: 0x5a4a60,
    wallHighlight: 0x7a6a80,

    // Lines
    centerLineColor: 0xffcc22,
    edgeColor: 0xffffff,

    // Ground
    grassColor: 0x2d5a1e,

    // Wall bounce (speed multiplier when hitting a wall)
    wallBounce: 0.55,
};
